import { BetterCustomDirectiveDirective } from './better-custom-directive.directive';

describe('BetterCustomDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new BetterCustomDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
